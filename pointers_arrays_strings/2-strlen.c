#include "main.h"
#include <string.h>

/**
 * _strlen - Obtains length of string.
 *
 * @s: String in which length is being determined.
 *
 * Return: Length of s.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
